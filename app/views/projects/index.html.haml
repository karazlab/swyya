.row
  #heading-with-line.col-12
    %h5
      = t(:listed_projects)
  .col-12
    .row
      .col
        = render partial: 'projects/partials/projects_list', locals: { projects: @projects, length: 200 }
    .row
      .mx-auto
        .apple_pagination
          = will_paginate @projects
