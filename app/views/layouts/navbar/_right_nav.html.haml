%ul.navbar-nav.ml-auto
  %li.nav-item
    = link_to projects_path, class: 'navbar-link' do
      = icon('fas', 'project-diagram', t(:projects))
  -# %li.nav-item
  -#   = link_to ideas_path, class: 'navbar-link' do
  -#     = icon('fas', 'brain', t(:ideas))
  - unless user_signed_in?
    %li.nav-item
      = link_to new_user_session_path, class: 'navbar-link' do
        = icon('fas', 'sign-in-alt', t(:login))
    %li.nav-item
      = link_to new_user_registration_path, class: 'navbar-link' do
        = icon('fas', 'user-plus', t(:register))
  - if user_signed_in?
    %li.nav-item.dropdown
      = link_to '#', class: 'navbar-link', id: 'navbarDropdown', 'aria-expanded': false, 'aria-haspopup': true, 'data-toggle': 'dropdown' do
        = icon('fas', 'stream', t(:menu))
      .dropdown-menu.dropdown-menu-right{"aria-labelledby" => "navbarDropdown"}
        = link_to new_project_path, class: 'dropdown-item navbar-dropdown' do
          = icon('fas', 'project-diagram', t(:new_project))
        -# .dropdown-divider
        -# = link_to new_idea_path, class: 'dropdown-item navbar-dropdown' do
        -#   = icon('fas', 'brain', t(:new_idea))
        .dropdown-divider
        = link_to user_board_path, class: 'dropdown-item navbar-dropdown' do
          = icon('fas', 'clipboard-list', t(:board))
        .dropdown-divider
        = link_to edit_user_registration_path, class: 'dropdown-item navbar-dropdown' do
          = icon('fas', 'user-edit', t(:edit_profile))
        .dropdown-divider
        = link_to destroy_user_session_path, method: :delete, class: 'dropdown-item navbar-dropdown' do
          = icon('fas', 'sign-out-alt', t(:logout))
