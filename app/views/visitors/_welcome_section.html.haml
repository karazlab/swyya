.row
  #welcome-section
    .jumbotron
      .row
        .col
          %h1.display-3
            Swyya
            %small
              Together we build
          %p.lead
            = t(:swyya_description)
      .row
        .col-12
          %hr
            .my-4
              = form_tag search_results_path, method: :get do
                #search
                  .input-group
                    = text_field_tag :search, params[:search], { class: 'form-control', placeholder: t(:index_search) }
                    .input-group-append
                      = submit_tag t(:search), class: 'btn btn-outline-primary search-btn'
